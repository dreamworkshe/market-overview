---
description: 當使用者說「紀錄」時，更新系統架構並推送至 GitHub
---

1. 檢查目前專案中的所有腳本 (`scripts/`)、數據結構 (`data/`) 以及自動化流程 (`.github/workflows/`)。
2. 根據目前的專案狀態，更新 `/Users/zero/Documents/Code/Market Overview/ARCHITECTURE.md` 的內容，確保其準確反映目前的技術棧與運作邏輯。
// turbo
3. 執行 git 指令將變更提交並推送到遠端倉庫：
```bash
git add ARCHITECTURE.md
git commit -m "docs: Update system architecture (record)"
git push origin main
```
